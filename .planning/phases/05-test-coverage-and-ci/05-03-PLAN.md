---
phase: 05-test-coverage-and-ci
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - tests/test_judge_integration.py
  - tests/test_live_smoke.py
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "The judge LLM dispatch path parses a fenced-JSON response into an ok DimensionScore with judge_method='llm'"
    - "A JSONDecodeError on the first attempts triggers retry and succeeds when a later attempt returns valid JSON"
    - "Retry exhaustion produces a status='error' DimensionScore (error_type set, score is None) via asyncio.gather, not a silent score=0.0 (F-A guard)"
    - "The fence-stripper handles a closing-fence response and fails-then-retries on a no-closing-fence multi-line response (F-I guard)"
    - "asyncio.gather aggregates 4 ok dims + 1 raising dim into 4 ok + 1 status='error' (F-A guard)"
    - "Live smoke tests are marker-skipped by default and only run under pytest -m live"
  artifacts:
    - path: "tests/test_judge_integration.py"
      provides: "judge.py LLM dispatch, retry, fence-strip, and gather-aggregation tests"
      contains: "async def test_evaluate_dimension_llm_path"
    - path: "tests/test_live_smoke.py"
      provides: "marker-skipped live API smoke tests (D3)"
      contains: "pytest.mark.live"
  key_links:
    - from: "tests/test_judge_integration.py"
      to: "agent_evaluator.judge.AnthropicJudge._evaluate_dimension / evaluate_trajectory"
      via: "AnthropicJudge(client=FixtureAnthropicClient(...), max_retries=2)"
      pattern: "AnthropicJudge\\(client="
    - from: "tests/test_live_smoke.py"
      to: "real Anthropic/OpenAI APIs"
      via: "@pytest.mark.live gated; skipped in CI via -m 'not live'"
      pattern: "@pytest.mark.live"
---

<objective>
Write the judge.py LLM-path integration tests (TEST-02) and the marker-skipped live smoke tests (D3). The judge tests exercise the fenced-JSON parse, the retry-on-JSONDecodeError loop, retry exhaustion routing to status="error" (F-A), the fence-stripper edge cases (F-I), and asyncio.gather aggregation of mixed ok/error dimensions. The live tests are opt-in (`pytest -m live`) and never run in CI.

Purpose: `judge.py` is the other load-bearing module flagged by F-F. These tests lock in the Phase 1 status-aware error path so a transient judge failure can never be confused with a legitimate zero. The live smoke tests give the user the structured opt-in handoff to validate end-to-end against real APIs at v1 closure.

Output: `tests/test_judge_integration.py` and `tests/test_live_smoke.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-test-coverage-and-ci/05-CONTEXT.md
@.planning/phases/05-test-coverage-and-ci/05-PATTERNS.md
@.planning/phases/05-test-coverage-and-ci/05-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: judge.py LLM-path integration tests (TEST-02, F-A / F-I guards)</name>
  <files>tests/test_judge_integration.py</files>
  <read_first>
    - tests/conftest.py (FixtureAnthropicClient created in Plan 01; replays the judge_response_ok.json and judge_response_malformed.json fixtures in queue order)
    - .planning/phases/05-test-coverage-and-ci/05-PATTERNS.md (the "tests/test_judge_integration.py" section has the exact test scaffolds: LLM happy path, retry, retry exhaustion → status="error", fence-stripper notes, gather-mixed)
    - src/agent_evaluator/judge.py (lines 36-44 AnthropicJudge.__init__ accepts client=/model=/max_retries=; lines 47-77 evaluate_trajectory gather loop that wraps Exceptions into status="error" with error_type; the _evaluate_dimension LLM dispatch and its retry loop bounded by max_retries; the _parse_score fence-stripping logic)
    - tests/test_judge.py (lines 18-69 inline _scenario_no_injection / _scenario_with_injection / _empty_trajectory factories and async test conventions — reuse these patterns; lines 87-234 async tests are the exact role analog)
    - src/agent_evaluator/rubrics.py (RUBRICS — which dimension names are LLM-judged vs deterministic after Phase 4, so the gather test asserts on a genuinely LLM-judged dim such as parameter_quality)
  </read_first>
  <action>
    Create `tests/test_judge_integration.py`. Reuse the inline scenario/trajectory factory pattern from test_judge.py. Construct judges as `AnthropicJudge(client=FixtureAnthropicClient([...]), model="claude-test", max_retries=2)`. Write these `@pytest.mark.asyncio` tests:
    - `test_evaluate_dimension_llm_path_parses_fenced_json`: client replays `judge_response_ok.json`; call `_evaluate_dimension` on an LLM-judged dimension (e.g. parameter_quality); assert `result.status == "ok"`, `result.judge_method == "llm"`, `result.score == pytest.approx(0.9)`, and that `client.calls[0]["model"] == "claude-test"`.
    - `test_evaluate_dimension_retries_on_malformed_json`: client replays malformed, malformed, then ok; assert the final result status is "ok" and `len(client.calls) == 3` (retried twice then succeeded — bounded by max_retries=2).
    - `test_evaluate_dimension_exhausts_retries_produces_error_status` (F-A guard): client replays three malformed responses; drive the path through `evaluate_trajectory` so asyncio.gather wraps the resulting ValueError; locate the parameter_quality DimensionScore in the result and assert `status == "error"`, `error_type == "ValueError"`, and `score is None` — explicitly NOT a silent score=0.0 masquerading as ok.
    - `test_parse_score_fence_variations` (F-I guard): assert the fence-stripper parses a standard closing-fence response, and that a fenced multi-line response with NO closing fence fails to parse (json.loads raises) → so it triggers retry rather than silently returning corrupt data. Author the no-closing-fence input inline (or via an extra fixture) per the PATTERNS F-I notes; assert the observable outcome (retry fired or ValueError raised), not internal state.
    - `test_gather_mixed_ok_and_error` (F-A guard): run `evaluate_trajectory` where one LLM dim's fixture is malformed-to-exhaustion and the others (including deterministic dims) succeed; assert the result has exactly one DimensionScore with status="error" and the rest are ok/na, mirroring judge.py lines 59-77.
    Assert via observable return values (status, judge_method, score, error_type, client.calls length) — never fragile internal attributes.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_judge_integration.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_judge_integration.py -q` shows all tests passed
    - `test_evaluate_dimension_llm_path_parses_fenced_json` asserts status=="ok", judge_method=="llm", score≈0.9
    - `test_evaluate_dimension_retries_on_malformed_json` asserts `len(client.calls) == 3`
    - `test_evaluate_dimension_exhausts_retries_produces_error_status` asserts the parameter_quality score has `status == "error"` and `error_type == "ValueError"` (F-A: not silent zero)
    - `test_gather_mixed_ok_and_error` asserts exactly one error-status dimension and the rest non-error
    - `ruff check tests/test_judge_integration.py` is clean
  </acceptance_criteria>
  <done>tests/test_judge_integration.py covers LLM dispatch, retry, retry-exhaustion→status="error" (F-A), fence-strip variations (F-I), and gather mixed aggregation; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: marker-skipped live smoke tests (TEST-02 live opt-in, D3)</name>
  <files>tests/test_live_smoke.py</files>
  <read_first>
    - .planning/phases/05-test-coverage-and-ci/05-CONTEXT.md (D3 — the three live test bodies and rationale; open-question recommendation #3 → do NOT add a _cmd_compare live test)
    - .planning/phases/05-test-coverage-and-ci/05-PATTERNS.md (the "tests/test_live_smoke.py" section — marker+async pattern and scenario-loading note; verify the actual registry API before writing imports)
    - src/agent_evaluator/judge.py (AnthropicJudge, OpenAIJudge, make_judge — confirm make_judge exists and returns OpenAIJudge for an OpenAI-prefixed model; confirm _evaluate_dimension signature)
    - scenarios/registry.py (confirm the real scenario-loading API — register/iter; the PATTERNS note flags load_scenario vs get_scenario as unverified, so check before importing)
    - scenarios/weather_lookup.py (a real no-injection scenario for the short-circuit test)
  </read_first>
  <action>
    Create `tests/test_live_smoke.py` with exactly three tests, each decorated `@pytest.mark.live` and `@pytest.mark.asyncio` (do NOT add a fourth _cmd_compare live test per open-question recommendation #3). Verify the real scenario-loading API from scenarios/registry.py before writing imports — use whatever the registry actually exposes (do not assume a load_scenario function exists; the scenario can also be built directly from scenarios/weather_lookup.py's builder). The three tests:
    - `test_live_anthropic_judge_short_circuit`: construct a real `AnthropicJudge()` (uses ANTHROPIC_API_KEY), load the weather_lookup (no-injection) scenario, and call `_evaluate_dimension("error_recovery", ...)`; assert `result.status == "na"` and `result.judge_method == "deterministic"` — proving the Phase 2 short-circuit consumes no tokens for that dim.
    - `test_live_anthropic_judge_llm_dim`: construct `AnthropicJudge()`, call `_evaluate_dimension` on a genuinely LLM-judged dimension (e.g. parameter_quality) against a real trajectory; assert `status == "ok"`, `judge_method == "llm"`, and `0.0 <= score <= 1.0`.
    - `test_live_make_judge_routes_openai`: call `make_judge("gpt-4o")` (uses OPENAI_API_KEY) and assert `isinstance(judge, OpenAIJudge)` — proving the Phase 3 F-C dispatch closure.
    These tests are skipped by default; CI runs `pytest -m 'not live'`. They are the structured user opt-in for v1 closure. The file must import and collect cleanly even without API keys present (the live marker prevents execution, not import).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_live_smoke.py -m 'not live' -q 2>&1 | tail -5 && .venv/bin/python -m pytest tests/test_live_smoke.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_live_smoke.py --collect-only -q` collects exactly 3 tests with no import error (file imports cleanly without API keys)
    - `pytest tests/test_live_smoke.py -m 'not live' -q` runs 0 tests / all deselected (the live marker excludes them by default)
    - Each test carries both `@pytest.mark.live` and `@pytest.mark.asyncio` (`grep -c 'pytest.mark.live' tests/test_live_smoke.py` is 3)
    - No `_cmd_compare` live test is present (per open-question recommendation #3)
    - `ruff check tests/test_live_smoke.py` is clean
  </acceptance_criteria>
  <done>tests/test_live_smoke.py has exactly 3 marker-skipped live tests that collect cleanly, run 0 under `-m 'not live'`, and target the Phase 2 short-circuit, the LLM dispatch path, and the Phase 3 OpenAI routing; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture client → judge under test | Hermetic; default test run reads no API keys. |
| live tests → real Anthropic/OpenAI APIs | Crosses into the network and reads real API keys; gated behind the `live` marker and excluded from CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information disclosure | live API keys in CI logs | mitigate | Live tests are gated behind `@pytest.mark.live`; CI runs `pytest -m 'not live'` so real API keys are never required in CI and cannot leak into CI logs. The file imports cleanly without keys. |
| T-05-06 | Spoofing | judge error path (F-A) | mitigate | The retry-exhaustion test asserts status="error" with error_type, proving a transport failure cannot be spoofed as a legitimate score=0.0 ok result. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No NEW package introduced; tests use already-declared pytest/pytest-asyncio. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_judge_integration.py -q` → all pass
- `.venv/bin/python -m pytest tests/test_live_smoke.py --collect-only -q` → 3 collected, no import error
- `.venv/bin/python -m pytest -m 'not live' -q` → full suite green; live tests deselected
- `ruff check src/ scenarios/ tests/` → clean
</verification>

<success_criteria>
- TEST-02: judge LLM dispatch, retry, retry-exhaustion→status="error" (F-A), fence-strip variations (F-I), gather mixed aggregation
- D3: 3 marker-skipped live smoke tests collect cleanly, deselected under `-m 'not live'`, no _cmd_compare live test
- No regression; ruff clean
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage-and-ci/05-03-SUMMARY.md` when done.
</output>
