---
phase: 05-test-coverage-and-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/fixtures/anthropic/agent_response_tool_use.json
  - tests/fixtures/anthropic/agent_response_final.json
  - tests/fixtures/anthropic/agent_response_no_usage.json
  - tests/fixtures/anthropic/judge_response_ok.json
  - tests/fixtures/anthropic/judge_response_malformed.json
  - tests/fixtures/openai/agent_response_tool_use.json
  - tests/fixtures/openai/agent_response_final.json
  - pyproject.toml
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "Test files can import FixtureAnthropicClient and FixtureOpenAIClient from conftest without explicit import path"
    - "FixtureAnthropicClient returns SimpleNamespace-shaped responses matching the attribute paths runner.py and judge.py access"
    - "FixtureOpenAIClient returns choice.message as a SimpleNamespace (not raw dict) so the F-G round-trip into messages= does not break"
    - "An Anthropic fixture with no usage key yields usage=None (drives the F-H regression guard)"
    - "pytest recognizes the live marker without warnings"
    - "All 69 existing tests still pass after pyproject.toml change"
  artifacts:
    - path: "tests/conftest.py"
      provides: "FixtureAnthropicClient + FixtureOpenAIClient shared fixture-replay clients"
      contains: "class FixtureAnthropicClient"
    - path: "tests/fixtures/anthropic/judge_response_ok.json"
      provides: "Valid fenced-JSON judge response for the LLM happy path"
    - path: "tests/fixtures/openai/agent_response_tool_use.json"
      provides: "OpenAI tool-call turn fixture for the runner F-G guard"
    - path: "pyproject.toml"
      provides: "live pytest marker registration"
      contains: "markers"
  key_links:
    - from: "tests/conftest.py FixtureAnthropicClient.create"
      to: "runner.py response.usage.input_tokens / response.content[i].type"
      via: "SimpleNamespace attribute shape"
      pattern: "SimpleNamespace"
    - from: "tests/conftest.py FixtureOpenAIClient.create"
      to: "runner.py messages.append(choice.message)"
      via: "choice.message is a SimpleNamespace object"
      pattern: "SimpleNamespace\\(message"
---

<objective>
Build the shared test infrastructure for Phase 5: the `FixtureAnthropicClient` / `FixtureOpenAIClient` replay clients in `tests/conftest.py`, the hand-rolled JSON response fixtures they read, and the `live` pytest marker registration in `pyproject.toml`.

Purpose: Every integration test in Plans 02 and 03 depends on these fixture clients and JSON files. This is the dependency root for the phase — it defines the contracts (fixture client constructors, JSON shapes) that downstream test files consume. Building it first prevents downstream executors from re-deriving SDK response shapes.

Output: `tests/conftest.py`, seven fixture JSON files under `tests/fixtures/anthropic/` and `tests/fixtures/openai/`, and an updated `pyproject.toml` with the `live` marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-test-coverage-and-ci/05-CONTEXT.md
@.planning/phases/05-test-coverage-and-ci/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the live pytest marker</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current [tool.pytest.ini_options] block is lines 31-33: asyncio_mode and pythonpath only, no markers key)
    - .planning/phases/05-test-coverage-and-ci/05-CONTEXT.md (D3 — exact marker string)
    - .planning/phases/05-test-coverage-and-ci/05-PATTERNS.md (pyproject.toml section, target state)
  </read_first>
  <action>
    Add a `markers` array to the existing `[tool.pytest.ini_options]` table in pyproject.toml. Keep the existing `asyncio_mode = "auto"` and `pythonpath = ["src"]` keys unchanged. Add exactly one marker entry with the name `live` and the help string `requires live API keys; skipped by default. Run with \`pytest -m live\`.` (the marker name and description text are LOCKED by D3). Do not add any other markers. Do not change the build-system, project, dependencies, or ruff tables.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest --markers 2>&1 | grep -q "@pytest.mark.live" && echo MARKER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest --markers` lists a marker named `live`
    - `grep -c '^markers' pyproject.toml` is 1 (the markers key exists exactly once)
    - `.venv/bin/python -m pytest -m 'not live' -q` collects and passes all 69 existing tests (no marker-related warnings)
  </acceptance_criteria>
  <done>pyproject.toml registers the `live` marker with the D3 description; existing 69 tests still pass under `-m 'not live'`.</done>
</task>

<task type="auto">
  <name>Task 2: Author the hand-rolled fixture JSON files</name>
  <files>tests/fixtures/anthropic/agent_response_tool_use.json, tests/fixtures/anthropic/agent_response_final.json, tests/fixtures/anthropic/agent_response_no_usage.json, tests/fixtures/anthropic/judge_response_ok.json, tests/fixtures/anthropic/judge_response_malformed.json, tests/fixtures/openai/agent_response_tool_use.json, tests/fixtures/openai/agent_response_final.json</files>
  <read_first>
    - .planning/phases/05-test-coverage-and-ci/05-PATTERNS.md (sections "tests/fixtures/anthropic/*.json" and "tests/fixtures/openai/*.json" contain the exact JSON contents to write; the "SDK Response Shape Reference" table lists every attribute path that must be present)
    - src/agent_evaluator/runner.py (lines 159-213 Anthropic path, lines 247-298 OpenAI path — confirm the attribute paths the fixtures must satisfy)
    - src/agent_evaluator/judge.py (the LLM dispatch path that reads response.content[0].text)
  </read_first>
  <action>
    Create the `tests/fixtures/anthropic/` and `tests/fixtures/openai/` directories and write the seven JSON files using the exact contents specified in the PATTERNS.md fixture sections. Required shapes:
    - Anthropic `agent_response_tool_use.json`: a `content` array with one `text` block and one `tool_use` block (fields `type`, `id`, `name`, `input`), a `usage` object with `input_tokens` and `output_tokens`, and `stop_reason`.
    - Anthropic `agent_response_final.json`: a `content` array with one `text` block, `usage` with `input_tokens`/`output_tokens`, `stop_reason`.
    - Anthropic `agent_response_no_usage.json`: a `content` array with one `text` block and `stop_reason`, and NO `usage` key (this is the F-H driver — FixtureAnthropicClient must yield `usage=None`).
    - Anthropic `judge_response_ok.json`: a single `text` block whose text is a fenced ```json block containing keys `score` (0.9), `reasoning`, `evidence`.
    - Anthropic `judge_response_malformed.json`: a single `text` block whose text is a fenced block containing intentionally invalid JSON so `json.loads` raises.
    - OpenAI `agent_response_tool_use.json`: `choices[0].message.tool_calls` with one entry (`id`, `function.name`, `function.arguments` as a JSON string), `content` null, and `usage` with `prompt_tokens`/`completion_tokens`.
    - OpenAI `agent_response_final.json`: `choices[0].message` with empty `tool_calls` and a string `content`, plus `usage`.
    Use the weather-lookup domain values from PATTERNS.md for brevity. Every file must be valid JSON parseable by `json.loads`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json,glob; files=sorted(glob.glob('tests/fixtures/anthropic/*.json')+glob.glob('tests/fixtures/openai/*.json')); [json.loads(open(f).read()) for f in files]; assert len(files)==7, files; print('FIXTURES_OK', len(files))"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 fixture files exist and parse as valid JSON
    - `tests/fixtures/anthropic/agent_response_no_usage.json` has no top-level `usage` key (verify: `python -c "import json; assert 'usage' not in json.load(open('tests/fixtures/anthropic/agent_response_no_usage.json'))"`)
    - `tests/fixtures/anthropic/judge_response_ok.json` content[0].text contains a fenced json block whose inner JSON has a `score` of 0.9 (parse the fenced body and assert)
    - `tests/fixtures/anthropic/judge_response_malformed.json` content[0].text, after stripping ``` fences, raises on `json.loads`
    - `tests/fixtures/openai/agent_response_tool_use.json` choices[0].message.tool_calls[0].function.arguments is a JSON string (str type, not dict)
  </acceptance_criteria>
  <done>Seven valid fixture JSON files exist with the SDK shapes that runner.py and judge.py access; the no-usage and malformed fixtures encode their regression-guard conditions.</done>
</task>

<task type="auto">
  <name>Task 3: Build FixtureAnthropicClient and FixtureOpenAIClient in conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - .planning/phases/05-test-coverage-and-ci/05-PATTERNS.md (the "tests/conftest.py — FixtureAnthropicClient + FixtureOpenAIClient" section has the exact class bodies to write, including the F-G note that choice.message must be a SimpleNamespace)
    - tests/test_judge.py (lines 72-84 _FakeAnthropicClient and 138-142 _FakeOpenAIClient — the existing fake-client conventions to extend; messages property returns self, async create)
    - src/agent_evaluator/runner.py (lines 159-298 — the attribute paths the SimpleNamespace responses must satisfy)
  </read_first>
  <action>
    Create `tests/conftest.py` with two classes, `FixtureAnthropicClient` and `FixtureOpenAIClient`, using the exact patterns in PATTERNS.md. Requirements:
    - Both constructors take `fixture_paths: list[Path]`, eagerly `json.loads` each file into a `self.fixtures` list, init `self._idx = 0` and `self.calls = []`.
    - `FixtureAnthropicClient`: a `messages` property returning `self`; an async `create(**kwargs)` that appends kwargs to `self.calls`, raises `StopIteration` when the queue is exhausted, else returns a `SimpleNamespace` with `content` (list of SimpleNamespace blocks built from each dict), `usage` (a SimpleNamespace if the fixture has a `usage` key, else `None`), and `stop_reason`.
    - `FixtureOpenAIClient`: `chat` and `completions` properties both returning `self`; an async `create(**kwargs)` that appends kwargs to `self.calls`, raises `StopIteration` when exhausted, else builds `response.choices[0].message` as a SimpleNamespace with `tool_calls` (a list of SimpleNamespace with `id` and a nested `function` SimpleNamespace carrying `name` and `arguments`, or `None` if empty) and `content`, plus `usage` (SimpleNamespace or None). The `choice.message` MUST be a SimpleNamespace object, NOT a raw dict — this is the F-G regression-guard requirement (runner.py line 267 appends `choice.message` directly into the next turn's `messages=`).
    - Place these as module-level classes in conftest.py so pytest auto-discovers the module and any test file can import them via `from conftest import FixtureAnthropicClient` or directly (per open-question recommendation D1 → conftest.py).
    - Do NOT define them as `@pytest.fixture`; they are plain classes instantiated by tests with explicit fixture-path lists.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys; sys.path.insert(0,'tests'); from pathlib import Path; from conftest import FixtureAnthropicClient, FixtureOpenAIClient; import asyncio; a=FixtureAnthropicClient([Path('tests/fixtures/anthropic/agent_response_tool_use.json'),Path('tests/fixtures/anthropic/agent_response_no_usage.json')]); r=asyncio.run(a.messages.create(model='x')); assert any(b.type=='tool_use' for b in r.content); assert r.usage.input_tokens==150 or r.usage.input_tokens>0; r2=asyncio.run(a.messages.create(model='x')); assert r2.usage is None; o=FixtureOpenAIClient([Path('tests/fixtures/openai/agent_response_tool_use.json')]); ro=asyncio.run(o.chat.completions.create(model='x')); m=ro.choices[0].message; from types import SimpleNamespace; assert isinstance(m, SimpleNamespace); assert m.tool_calls[0].function.name=='get_weather'; print('CONFTEST_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from conftest import FixtureAnthropicClient, FixtureOpenAIClient` succeeds with tests/ on sys.path
    - FixtureAnthropicClient.create returns an object where `.content[i].type` and `.content[i].text` are accessible and `.usage` is None for the no_usage fixture
    - FixtureOpenAIClient.create returns an object where `choices[0].message` is a `types.SimpleNamespace` (asserted via isinstance) and `choices[0].message.tool_calls[0].function.arguments` is the JSON string from the fixture
    - Both clients raise StopIteration when called more times than fixtures provided
    - `.venv/bin/python -m pytest -m 'not live' -q` still collects and passes all 69 existing tests (conftest does not break collection)
  </acceptance_criteria>
  <done>tests/conftest.py exposes both fixture-replay clients with SDK-accurate SimpleNamespace shapes; the OpenAI client returns SimpleNamespace messages for the F-G round-trip; existing tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture JSON → test process | Static author-written JSON files; no untrusted input. Read by FixtureClient via json.loads. |
| package install (`pip install -e ".[dev]"`) | Dev dependencies pytest/pytest-asyncio/ruff resolved from PyPI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | fixture JSON files | accept | Author-written static test fixtures, not runtime input; no PII, no secrets. Parsed only in the test process. |
| T-05-02 | Information disclosure | fixture clients (no real API keys) | mitigate | FixtureAnthropicClient/FixtureOpenAIClient never construct a real SDK client and never read env API keys; hermetic by construction. |
| T-05-SC | Tampering | dev deps (pytest, pytest-asyncio, ruff) | accept | All three are already declared in pyproject.toml `[project.optional-dependencies].dev` and in use by the existing 69-test suite; no NEW package introduced by this plan. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -m 'not live' -q` → 69 passed, 0 failed, no marker warnings
- All 7 fixture JSON files parse; conftest importable; both fixture clients return SDK-shaped objects
- `ruff check src/ scenarios/ tests/` → clean
</verification>

<success_criteria>
- live marker registered in pyproject.toml (D3 text)
- 7 hand-rolled fixture JSON files exist with correct SDK shapes (D1)
- FixtureAnthropicClient + FixtureOpenAIClient in conftest.py, OpenAI client yields SimpleNamespace messages (F-G prerequisite)
- 69 existing tests still pass; ruff clean
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage-and-ci/05-01-SUMMARY.md` when done.
</output>
