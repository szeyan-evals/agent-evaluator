---
phase: 05-test-coverage-and-ci
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - tests/test_runner_integration.py
  - tests/test_report.py
autonomous: true
requirements: [TEST-01, TEST-03]
must_haves:
  truths:
    - "A fixture-backed _run_anthropic multi-turn loop produces an AgentTrajectory with the expected step count, final answer, and summed token totals"
    - "The _run_openai loop round-trips choice.message (SimpleNamespace) into the next turn's messages= without breaking (F-G regression guard)"
    - "An Anthropic loop with usage=None is captured as a strict-xfail documenting the F-H AttributeError"
    - "An Anthropic loop fed more tool_use turns than max_reasonable_steps+5 terminates with final_answer None (max-steps grace verified)"
    - "generate_report renders empty, all-ok, partial (asterisk + footnote), and all-legacy cases correctly"
    - "generate_comparison_report marks partial cells and emits a footnote"
  artifacts:
    - path: "tests/test_runner_integration.py"
      provides: "runner.py _run_anthropic / _run_openai integration tests with F-G/F-H/max-steps guards"
      contains: "async def test_run_anthropic_happy_path"
    - path: "tests/test_report.py"
      provides: "report.py generate_report / generate_comparison_report edge-case tests"
      contains: "def test_generate_report_empty"
  key_links:
    - from: "tests/test_runner_integration.py"
      to: "agent_evaluator.runner.AgentRunner._run_anthropic / _run_openai"
      via: "AgentRunner.__new__ client injection with FixtureClient"
      pattern: "AgentRunner.__new__"
    - from: "tests/test_report.py"
      to: "agent_evaluator.report.generate_report / generate_comparison_report"
      via: "inline EvaluationResult construction"
      pattern: "generate_report|generate_comparison_report"
---

<objective>
Write the runner.py integration tests (TEST-01) and the report.py edge-case tests (TEST-03). The runner tests exercise the Anthropic and OpenAI agentic loops against the Plan-01 fixture clients, including the F-G (OpenAI choice.message round-trip), F-H (Anthropic usage=None), and max-steps regression guards. The report tests exercise rendering of empty / all-ok / partial / all-legacy / comparison-partial cases.

Purpose: `runner.py` and `report.py` are two of the load-bearing modules called out by F-F as having zero integration coverage. These tests lock in the Phase 1-4 behavior so regressions get caught.

Output: `tests/test_runner_integration.py` and `tests/test_report.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-test-coverage-and-ci/05-CONTEXT.md
@.planning/phases/05-test-coverage-and-ci/05-PATTERNS.md
@.planning/phases/05-test-coverage-and-ci/05-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: runner.py integration tests (TEST-01, F-G / F-H / max-steps guards)</name>
  <files>tests/test_runner_integration.py</files>
  <read_first>
    - tests/conftest.py (FixtureAnthropicClient, FixtureOpenAIClient — created in Plan 01; the constructors take list[Path] and replay fixtures)
    - .planning/phases/05-test-coverage-and-ci/05-PATTERNS.md (the "tests/test_runner_integration.py" section has the exact test scaffolds: AgentRunner.__new__ injection, happy-path assertions, the strict-xfail F-H test, the max-steps test)
    - src/agent_evaluator/runner.py (lines 124-133 __init__ shows the client is built eagerly so tests must use AgentRunner.__new__ and set .model, ._use_openai, ._anthropic_client / ._openai_client; lines 144-298 the two loop bodies; line 152 max_steps = scenario.max_reasonable_steps + 5; line 166 response.usage.input_tokens; line 267 messages.append(choice.message))
    - tests/test_judge.py (lines 18-61 inline scenario/trajectory factories; lines 87-102 async test structure with @pytest.mark.asyncio)
    - src/agent_evaluator/models.py (Scenario, AgentTrajectory, ToolDefinition, MockResponse field shapes for the inline scenario factory)
    - scenarios/weather_lookup.py (a real small scenario with mock_responses to mirror in the inline factory)
  </read_first>
  <action>
    Create `tests/test_runner_integration.py`. Import the fixture clients from conftest and build a small module-level `_weather_scenario()` factory (a Scenario with available_tools including a `get_weather` ToolDefinition and matching mock_responses, error_injection empty, a small max_reasonable_steps such as 3). Inject the fixture client via `runner = AgentRunner.__new__(AgentRunner); runner.model = "claude-test"; runner._use_openai = False; runner._anthropic_client = FixtureAnthropicClient([...])` (and the OpenAI analog with `runner._use_openai = True; runner._openai_client = FixtureOpenAIClient([...])`). Write these tests, all decorated `@pytest.mark.asyncio`:
    - `test_run_anthropic_happy_path`: feed `agent_response_tool_use.json` then `agent_response_final.json`; assert the result isinstance AgentTrajectory, has the expected number of steps (one tool-using step), final_answer is not None, and total input tokens equal the sum of the two fixtures' input_tokens.
    - `test_run_anthropic_usage_none_raises`: decorate with `@pytest.mark.xfail(strict=True, reason="F-H: usage=None raises AttributeError; fix deferred to post-v1")` (per open-question recommendation D2 → strict xfail). Feed `agent_response_no_usage.json` and call `_run_anthropic`; expect AttributeError at runner.py line 166.
    - `test_run_anthropic_max_steps_terminates`: feed more `agent_response_tool_use.json` fixtures than `max_reasonable_steps + 5` (with max_reasonable_steps=3 that is 9+ tool_use turns); assert the loop exhausts and `trajectory.final_answer is None`.
    - `test_run_openai_happy_path`: feed OpenAI `agent_response_tool_use.json` then `agent_response_final.json`; assert AgentTrajectory shape and token totals (prompt_tokens summed).
    - `test_run_openai_choice_message_round_trip` (F-G guard): feed the OpenAI tool_use turn then the final turn; assert the loop completes without error AND that the FixtureOpenAIClient recorded a second call whose `messages` kwarg contains the prior turn's `choice.message` object (i.e. the SimpleNamespace appended at runner.py line 267 survived the round-trip into the next `create(messages=...)`). If the runner instead raises because choice.message is not append-compatible, the test must fail loudly with a clear assertion message.
    Use module-level `def _...()` helpers, not @pytest.fixture (matches existing test_judge.py convention).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_runner_integration.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_runner_integration.py -q` reports the happy-path, max-steps, and F-G tests as passed and the F-H test as xfailed (xpassed would fail the strict flag — that is the intended signal if the bug is incidentally fixed)
    - `test_run_anthropic_happy_path` asserts `trajectory.total_input_tokens == 350` (150 + 200 from the two fixtures) or the equivalent summed value matching the authored fixtures
    - `test_run_openai_choice_message_round_trip` asserts the recorded second-call `messages` kwarg includes the SimpleNamespace returned for the prior turn (F-G round-trip proven)
    - `test_run_anthropic_max_steps_terminates` asserts `trajectory.final_answer is None`
    - `ruff check tests/test_runner_integration.py` is clean
  </acceptance_criteria>
  <done>tests/test_runner_integration.py covers both runner loops with passing happy-path/max-steps/F-G tests and a strict-xfail F-H guard; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: report.py edge-case tests (TEST-03)</name>
  <files>tests/test_report.py</files>
  <read_first>
    - .planning/phases/05-test-coverage-and-ci/05-PATTERNS.md (the "tests/test_report.py" section has the corrected _make_result factory and each test body: empty, all-ok, partial row, all-legacy, comparison partial — note the dimension-name and asterisk-assertion warnings inline in that section)
    - src/agent_evaluator/rubrics.py (lines 61-246 RUBRICS dict — the SOURCE OF TRUTH for dimension names; confirm the five keys before authoring the factory)
    - src/agent_evaluator/report.py (generate_report and generate_comparison_report — confirm the exact strings rendered: empty-dir message, "No non-legacy results to report", "Legacy evaluations excluded", "Partial evaluations" footnote, the asterisk/partial-cell rendering in _render_dim_cell / _render_overall_cell)
    - src/agent_evaluator/models.py (DimensionScore and EvaluationResult constructors — fields schema_version, legacy, scenario_id, model_id, dimension_scores, overall_score, summary, and DimensionScore status/error_type for partial rows)
    - tests/test_models.py (synchronous unit-test style with inline construction — the analog for these sync tests)
  </read_first>
  <action>
    Create `tests/test_report.py` (synchronous tests, no asyncio).

    FIRST, confirm the five dimension names directly from `src/agent_evaluator/rubrics.py` RUBRICS keys before authoring the `_make_result` factory. They are: `tool_selection`, `parameter_quality`, `efficiency`, `error_recovery`, `final_correctness`. There is NO `final_answer_quality` — that name is fictional and a score under it would be silently dropped by `generate_report` (which iterates RUBRICS.keys() at report.py line 94), since `DimensionScore.dimension` is a free-form str that Pydantic will not reject. Use ONLY the five real names.

    Add a module-level `_make_result(...)` factory matching the models.py EvaluationResult schema (defaults: schema_version=3, legacy=False, five ok DimensionScores over the five real dimension names above, overall_score=0.8).

    Read the actual rendered strings from report.py and assert against them exactly — do NOT guess, and do NOT gate on bare `"*"`. Note: `generate_report` UNCONDITIONALLY emits Markdown bold markers (`**Average**`, `**0.80**` via `_render_overall_cell` report.py line 33 and the averages row line 139), so `assert "*" not in report` is ALWAYS false — it is the wrong assertion. The partial signal is specific: the `**Partial evaluations:**` footnote heading (report.py line 45) and the `*` SUFFIX appended INSIDE a score cell (e.g. `0.80*` from `_render_dim_cell` line 27, or the partial overall marker on line 33). Target those specifics. Write these tests:
    - `test_generate_report_empty`: `generate_report([])` returns the empty-results message verbatim from report.py (the PATTERNS draft says "No results to report" — confirm against source line 77 and use the real string).
    - `test_generate_report_all_ok`: one all-ok result; assert the scenario id appears, `"Partial evaluations" not in report` (no footnote section), and that no partial score-suffix cell is present — assert the clean overall cell `"**0.80**"` is in the report and `assert "0.80*" not in report.replace("**", "")`. CAUTION: a bare `assert "0.80*" not in report` is INVALID — the bold cell `**0.80**` contains `0.80*` as a substring (digits + first closing-bold asterisk), so it would fail whenever the positive assertion passes. Strip `**` bold markers before the negative check so only a genuine partial suffix (dim cell `0.80*`, partial overall `**0.80***`) can match.
    - `test_generate_report_partial_row`: one result with four ok dims (drawn from the five real names) plus one DimensionScore `score=None status="error" error_type="ValueError"`; assert the partial footnote heading "Partial evaluations" appears, the errored cell renders `"err*"` (errored dims carry `score=None`, so the cell is the literal `err*`, NOT a number-with-asterisk), and the error_type "ValueError" surfaces.
    - `test_generate_report_all_legacy`: one result with schema_version=1 legacy=True; assert the legacy-only message and the legacy-excluded footnote appear (confirm exact strings from report.py lines 85, 62).
    - `test_generate_comparison_report_partial`: build a comparison input mapping two model ids to result lists (one ok, one partial) and assert the comparison output renders the errored cell as `"err*"` (errored dim has `score=None`) and emits the "Partial evaluations" footnote.
    Confirm the exact comparison-report input signature from report.py before writing (dict of model_id → list[EvaluationResult] per PATTERNS, but verify).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_report.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_report.py -q` shows all 5 tests passed
    - The `_make_result` factory uses ONLY the five real RUBRICS dimension names (tool_selection, parameter_quality, efficiency, error_recovery, final_correctness) — no `final_answer_quality`
    - The asserted strings (empty message, legacy messages, partial footnote heading) are byte-for-byte taken from report.py source, not invented
    - No test uses the invalid `assert "*" not in report` gate; partial-absence is proven via the absence of the score-suffix asterisk cell and the absence of the "Partial evaluations" footnote. Any negative score-suffix check whose digits also appear in a bold overall cell (e.g. `0.80`) MUST strip bold markers first (`report.replace("**", "")`) — a bare `"0.80*" not in report` collides with `**0.80**` and always fails
    - `test_generate_report_partial_row` asserts both a score-suffix asterisk marker and the literal "ValueError" appear in the output
    - `ruff check tests/test_report.py` is clean
  </acceptance_criteria>
  <done>tests/test_report.py covers empty, all-ok, partial, all-legacy, and comparison-partial rendering with the five real dimension names and assertions on the real report.py strings (no bare-asterisk gate); ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture clients → runner under test | Hermetic; no network, no API keys. |
| EvaluationResult construction → report rendering | In-process Pydantic objects; no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information disclosure | runner integration tests | mitigate | Tests inject FixtureAnthropicClient/FixtureOpenAIClient via AgentRunner.__new__, never constructing a real SDK client; no env API key is read, so keys cannot leak into test logs or CI. |
| T-05-04 | Denial of service | max-steps loop test | accept | The max-steps test feeds a bounded fixture queue and asserts the runner's `max_reasonable_steps + 5` grace terminates the loop; no unbounded iteration. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No NEW package introduced; tests use already-declared pytest/pytest-asyncio. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_runner_integration.py tests/test_report.py -q` → all pass, F-H test xfailed
- `.venv/bin/python -m pytest -m 'not live' -q` → full suite green (69 prior + new)
- `ruff check src/ scenarios/ tests/` → clean
</verification>

<success_criteria>
- TEST-01: runner _run_anthropic and _run_openai integration tests with F-G round-trip, F-H strict-xfail, and max-steps guards
- TEST-03: report empty / all-ok / partial / all-legacy / comparison-partial tests asserting real report.py strings with the correct five dimension names and specific (non-bare-asterisk) partial assertions
- No regression in the existing suite; ruff clean
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage-and-ci/05-02-SUMMARY.md` when done.
</output>
